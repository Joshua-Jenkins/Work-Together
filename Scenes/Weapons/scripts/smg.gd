# hand_gun.gd (Godot 4.5.1) — CORRECT
# This weapon uses ALL logic from WeaponBase.gd (aim, rotate, shoot, bullet hit + knockback).
# Only use this script to identify the weapon type and optionally set defaults in the Inspector.

extends WeaponBase
class_name SMG
